package com.ilCary.CarUnit.repo;

import com.ilCary.CarUnit.models.Modello;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ModelloRepo extends JpaRepository<Modello, Long> {
}
